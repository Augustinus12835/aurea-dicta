# Aurea Dicta - Automated Lecture-to-Video Pipeline
# Python dependencies for educational video creation workflow

# Core utilities
requests>=2.28.0
python-dotenv>=1.0.0
pyyaml>=6.0.0             # Configuration file parsing

# AI/ML APIs
anthropic>=0.39.0         # Claude API client (segmentation, scripts)
google-generativeai>=0.8.0  # Gemini API (image generation)

# Audio/Video processing
mutagen>=1.47.0           # MP3 metadata reading
openai-whisper>=20231117  # Audio transcription for subtitles

# Data analysis and visualization
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
scipy>=1.10.0             # Statistical analysis
openpyxl>=3.1.0           # Excel file support for pandas

# Financial data APIs (optional, for data visualization)
yfinance>=0.2.30          # Yahoo Finance market data
fredapi>=0.5.1            # FRED economic data

# Text-to-Speech APIs
elevenlabs>=1.0.0         # ElevenLabs TTS (voice cloning support)

# PDF generation
reportlab>=4.0.0          # PDF study guide generation
pillow>=9.0.0             # Image handling for PDFs

# SSL certificates
certifi>=2023.0.0

# Note: The following system-level dependencies are also required:
# - ffmpeg/ffprobe: Video compilation (install via: sudo apt install ffmpeg)
# - poppler-utils: PDF extraction (install via: sudo apt install poppler-utils)
